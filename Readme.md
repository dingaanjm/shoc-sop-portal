# SHOC SOP Portal

Streamlit portal for SHOC Main SOPs, Toolkits and Templates.

## Run locally
```bash
python -m pip install -r requirements.txt
python -m streamlit run shoc_sop_portal_app_branded.py